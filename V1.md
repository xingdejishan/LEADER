# v1：LEADER baseline + 全池鲁棒重估

本分支的固定定义是：官方 LEADER baseline 网络与 checkpoint，加上推理端全池鲁棒位姿重估。

- 网络、场景坐标预测和可靠度预测保持官方 baseline 不变，不重新训练。
- 初始位姿保持官方流程：可靠度 Top-50% correspondence 输入 SC2-PCR。
- 获得初始位姿后，将候选池扩展到网络输出的全部 correspondence。
- 按 `2.0 → 1.2 → 0.8 → 0.6 m` 逐级收紧几何残差门限，使用 Tukey bisquare 权重和加权 SVD 反复重估刚体位姿。
- 推理过程不读取真值；真值只用于最终评测。

完整 NCLT 测试集 104735 帧上，官方 baseline 为 `0.30653 m / 1.75555°`，本分支方法为 `0.25293 m / 1.58386°`。当前定位是强工程基线，不将通用鲁棒重估本身声称为科研创新。
