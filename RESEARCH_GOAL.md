# GLACE → LEADER 研究目标

GLACE 的任务是为 LEADER 提供可靠的图像像素—世界三维坐标对应约束，发挥图像的互补信息。独立 GLACE 位姿精度、点级误差或重投影内点率只能作为中间诊断；最终验收看独立日期上相同定位门限下，融合救回的失败是否稳定多于新增失败，并同时检查严格门限和尾部误差。

当前开发分支：`research/glace-correspondence-improvement`。当前融合接口使用二维重投影误差；LiDAR 三维几何用于训练监督。不得将诊断中使用的 GT 监督目标或 oracle 筛点用于推理。

本轮按照用户方案优先落实固定候选、固定像素的替换回放，报告见 [固定回放报告](code/research/glace_fusion/replay_results_20260912/REPORT.md)。暂未获得可部署的稳定收益。

## 实施状态

| 要求 | 状态 |
|---|---|
| 固定真实六自由度候选，保留原始 LEADER 和 v1 输出 | 已实现、两组模型各 148 帧已运行 |
| 当前监督目标／预测／oracle 子集替换，匹配像素和覆盖 | 已实现；oracle 另有同数量、同空间块随机控制 |
| 独立核验参考点替换 | 接口已实现，本地缺少独立静态参考点，未伪造该结果 |
| 可达率、选中率、选错损失、重评分与优化后分开报告 | 已实现 |
| 出界／负深度保留分母、空间块限权、分块独立验收 | 已实现诊断版本；未通过验证集校准，不替换生产后端 |
| 打乱像素—世界点配对负对照 | 已运行 |
| 官方标定勘误、坐标链、缩放与输出网格核查 | 参数一致；物理投影与同步仍未独立核验 |
| 有符号残差、沿视线／横向／角度误差 | 已保存；303 帧验证日期完成纹理、远近和空间区域诊断 |
| 可信静态表面与视线交点监督 | 待独立参考与静态表面支持，当前三邻点深度目标不冒充可靠表面交点 |
| 方向／距离分别归一化的新损失及坐标训练 | 尚未改训练；先完成上述监督核验 |
| 点级可靠性＋真实候选难例排序训练 | 尚未训练；缺独立训练/校准日期的真实候选池，不能挖掘测试难例后反向训练 |
| 新坐标 × 新可靠性的交叉消融 | 回放基础设施已建立，新模型与可靠性尚未产生，不用旧模型冒充 |
| 独立日期最终融合验收 | 未完成；当前 2012-02-12 已多次用于开发，不能作新最终测试集 |

## 数据边界

- 坐标训练图像为 2012-01-22、2012-02-02、2012-05-11，共 907 张；本地现有三维文件覆盖 905 张。历史 selected 配置记录辅助图像 254 张，不能把本地后补数据覆盖率当成历史模型实际训练覆盖率。
- 2012-02-18 的 303 张图像已经用于历史模型选择和置信度开发，本轮仅诊断，不宣称新独立验证；后续需在训练日期内重新落实交叉拟合、可靠性训练与门限验证的隔离。
- 2012-02-12 的 148 张图像和候选只作已使用的开发回放，不用于新损失、可靠性模型或门限拟合。
- 本轮所有改进方案、分数和验收阈值均固定后运行，不根据测试表现搜索参数；分块门控仍出现损害，不能据此发布。

## 下一阶段所需输入

1. 少量静态物理角点/边缘或独立多视图三维参考，附独立核验方式、世界坐标约定、像素坐标及误差范围；不能仅由当前 GT 与同一 LiDAR 标签生成再投影验证。
2. 非测试日期的真实 LEADER 查询候选、三维对应池、相机预测缓存和参考位姿，用于可靠性难例训练与独立门限校准；必须记录该查询是否被坐标模型拟合过。
3. 尚未用于调参的测试日期，相同接口的 LiDAR/图像数据和参考质量信息；运动诊断还需轨迹速度、角速度或原始同步依据。

## 独立参考输入契约

参考目录内放置 `manifest.json`，其中 `verification` 为 `manual_physical_features` 或 `independent_multiview`，`provenance` 描述生成方法、静态核验及其与当前标签的独立性。声明本身不能替代人工审阅。

每帧为 `<image_timestamp>.npz`，包含 `uv[N,2]`、`xyz[N,3]`、`valid[N]`；`uv` 必须与原预测缓存的完整像素网格顺序一致，`xyz` 使用相同世界坐标系，仅 `valid=True` 的独立核验点参与对照。其余位置可保留占位点。只存在部分参考帧时，报告单列 `reference_available` 分组，且同帧其他方法使用相同像素重新比较。

```powershell
.\.venv\Scripts\python.exe local.py replay --reference path/to/verified-reference --out outputs/reference-replay
```
